<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{HTTP_HOST} ^www\.tumblersouvenir\.com [NC]
  RewriteRule ^(.*)$ https://tumblersouvenir.com/$1 [L,R=301]
  RewriteCond %{HTTPS} off
  RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>
